#include <stdio.h>
#include "mathcustomlib.h"

int main() {
    printf("hello!\n");
    printf("add: %d\n", add(5, 3));
    printf("sub: %d\n", sub(5, 3));
    printf("mul: %d\n", mul(2, 3));
    return 0;
}